"use client";

import { SocialSection } from "../sections/SocialSection";

/**
 * /admin/marketing/social — the Social Media Scheduler section.
 * Composition lives in `sections/SocialSection.tsx`; this file is the
 * routing entry-point only.
 */
export default function SocialPage() {
  return <SocialSection />;
}
