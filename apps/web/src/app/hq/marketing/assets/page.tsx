"use client";

import { AssetsSection } from "../sections/AssetsSection";

/**
 * /admin/marketing/assets — the Marketing Asset Library section.
 * Composition lives in `sections/AssetsSection.tsx`; this file is the
 * routing entry-point only.
 */
export default function AssetsPage() {
  return <AssetsSection />;
}
