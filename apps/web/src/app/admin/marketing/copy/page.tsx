"use client";

import { CopySection } from "../sections/CopySection";

/**
 * /admin/marketing/copy — the Copy Library section.
 * Composition lives in `sections/CopySection.tsx`; this file is the
 * routing entry-point only.
 */
export default function CopyPage() {
  return <CopySection />;
}
